const orm = require("../config/orm");

const manager = {
    addNew: reinit => {
        //add new manager
    },

    update: reinit => {
        //update manager
    },

    remove: reinit => {
        //remove manager
    }
}

module.exports = manager;
const orm = require("../config/orm");

const role = {
    addNew: reinit => {
        //add new role
    },

    update: reinit => {
        //update role
    },

    remove: reinit => {
        //remove role
    }
}

module.exports = role;
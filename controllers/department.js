const orm = require("../config/orm");

const department = {
    addNew: reinit => {
        //add new department
    },

    update: reinit => {
        //update department
    },

    remove: reinit => {
        //remove department
    }
}

module.exports = department;